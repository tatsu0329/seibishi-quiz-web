# 📚 問題集追加ガイド

このガイドでは、新しい問題集を追加する方法を詳しく説明します。

## 🎯 全体の流れ

1. JSONファイルを作成
2. 画像を配置（必要な場合）
3. `src/data/index.ts` を更新
4. 動作確認

---

## 📝 ステップ1: JSONファイルを作成

### 1-1. テンプレートをコピー

`src/data/questionSets/TEMPLATE.json` をコピーして、新しいファイル名にリネームします。

**ファイル名の例：**
- `level-1-C-2025-1.json` （国家1級 小型 2025年度第1回）
- `level-2-G-2025-2.json` （国家2級 ガソリン 2025年度第2回）
- `level-3-D-2024-1.json` （国家3級 ディーゼル 2024年度第1回）

### 1-2. ファイル名の命名規則

```
level-{等級番号}-{燃料タイプ}-{年度}-{回次}.json
```

**等級番号：**
- `1` = 国家1級
- `2` = 国家2級
- `3` = 国家3級

**燃料タイプ：**
- `C` = 小型（1級のみ）
- `G` = ガソリン
- `D` = ディーゼル

**年度と回次：**
- `2025-1` = 2025年度第1回
- `2025-2` = 2025年度第2回

### 1-3. JSONファイルを編集

テンプレートを開いて、以下の内容を編集します：

```json
{
  "category": {
    "level": "国家1級",        // ← 等級を設定
    "fuelType": "小型",        // ← 燃料タイプを設定
    "year": "2025-1"          // ← 年度と回次を設定
  },
  "questions": [
    {
      "id": "No.1",           // ← 問題ID（No.1, No.2, ...）
      "question": "問題文...", // ← 問題文を記入
      "choices": [            // ← 選択肢を4つ記入
        "(1). 選択肢1",
        "(2). 選択肢2",
        "(3). 選択肢3",
        "(4). 選択肢4"
      ],
      "answerIndex": 1,       // ← 正解の番号（1-4）
      "explanation": "解説...", // ← 解説（任意）
      "images": ["/image/..."] // ← 画像パス（任意）
    }
  ]
}
```

**重要：**
- `answerIndex` は **1ベース** です（1 = 選択肢(1)が正解）
- 選択肢は必ず **4つ** 必要です
- 問題ID（`id`）は一意である必要があります

---

## 🖼️ ステップ2: 画像を配置（必要な場合）

### 2-1. 画像ディレクトリを作成

`public/image/` ディレクトリに、年度・等級・燃料タイプに基づいたディレクトリを作成します。

**ディレクトリ名の規則：**
```
{年度下2桁}{等級番号}{燃料タイプ略称}
```

**例：**
- 2025年度第1回 国家1級 小型 → `25031c`
- 2024年度第2回 国家2級 ガソリン → `24032g`
- 2023年度第1回 国家3級 ディーゼル → `23033d`

### 2-2. 画像ファイルを配置

作成したディレクトリに画像ファイルを配置します。

**画像ファイル名の規則：**
```
{年度下2桁}{等級番号}{燃料タイプ略称}-{問題番号}.png
```

**例：**
- `25031c-1.png` （2025年度第1回 国家1級 小型 問題1の画像）
- `24032g-5.png` （2024年度第2回 国家2級 ガソリン 問題5の画像）

### 2-3. JSONファイルに画像パスを記入

```json
{
  "id": "No.1",
  "question": "問題文...",
  "choices": [...],
  "answerIndex": 1,
  "images": ["/image/25031c/25031c-1.png"]  // ← 画像パスを記入
}
```

**複数の画像がある場合：**
```json
"images": [
  "/image/25031c/25031c-1.png",
  "/image/25031c/25031c-1-2.png"
]
```

---

## 🔧 ステップ3: `src/data/index.ts` を更新

### 3-1. インポート文を追加

`src/data/index.ts` の先頭に、新しいJSONファイルのインポート文を追加します。

**例：`level-1-C-2025-1.json` を追加する場合**

```typescript
// 国家1級
import level1C20251 from "./questionSets/level-1-C-2025-1.json"; // ← 追加
import level1C2023 from "./questionSets/level-1-C-2023.json";
// ...
```

**インポート変数名の規則：**
- `level1C20251` = level-1-C-2025-1
- `level2G20242` = level-2-G-2024-2
- `level3D20231` = level-3-D-2023-1

### 3-2. エクスポート配列に追加

`export default [...]` の配列に、新しい問題集を追加します。

**例：**

```typescript
export default [
  // 国家1級
  level1C20251,  // ← 追加（新しい年度は上に配置）
  level1C2023,
  level1C20212,
  // ...
];
```

**推奨：** 新しい年度の問題集は配列の上に配置すると、選択画面で新しい年度が上に表示されます。

---

## ✅ ステップ4: 動作確認

### 4-1. 開発サーバーを起動

```bash
npm run dev
```

### 4-2. ブラウザで確認

1. ブラウザで `http://localhost:3000` にアクセス
2. 「問題集を選択して学習を始める」をクリック
3. `/select` ページで新しい問題集が表示されることを確認
4. 問題集を選択して、問題が正しく表示されることを確認
5. 画像が正しく表示されることを確認（画像を使用している場合）

---

## 📋 実践例：2025年度第2回 国家2級 ガソリンを追加する場合

### 1. JSONファイルを作成

**ファイル名：** `level-2-G-2025-2.json`

**内容：**
```json
{
  "category": {
    "level": "国家2級",
    "fuelType": "ガソリン",
    "year": "2025-2"
  },
  "questions": [
    {
      "id": "No.1",
      "question": "Q01. エンジンオイルに関する記述として、適切なものは次のうちどれか。",
      "choices": [
        "(1). エンジンオイルは、潤滑、冷却、密封、清浄の4つの役割を持つ。",
        "(2). エンジンオイルの粘度は、温度が高いほど高くなる。",
        "(3). エンジンオイルは、使用期間が長いほど粘度が高くなる。",
        "(4). エンジンオイルは、添加剤を含まない純粋な鉱物油である。"
      ],
      "answerIndex": 1,
      "explanation": "エンジンオイルは、潤滑、冷却、密封、清浄の4つの主要な役割を持ちます。"
    }
  ]
}
```

### 2. `src/data/index.ts` を更新

```typescript
// 国家2級 - ガソリン
import level2G20252 from "./questionSets/level-2-G-2025-2.json"; // ← 追加
import level2G20251 from "./questionSets/level-2-G-2025-1.json";
// ...

export default [
  // ...
  // 国家2級 - ガソリン
  level2G20252,  // ← 追加（新しい年度は上に）
  level2G20251,
  // ...
];
```

### 3. 動作確認

開発サーバーを再起動して、新しい問題集が表示されることを確認します。

---

## 🎨 画像の配置例

### 例：2025年度第1回 国家1級 小型の場合

1. **ディレクトリを作成：**
   ```
   public/image/25031c/
   ```

2. **画像ファイルを配置：**
   ```
   public/image/25031c/25031c-1.png
   public/image/25031c/25031c-2.png
   public/image/25031c/25031c-3.png
   ```

3. **JSONファイルに記入：**
   ```json
   {
     "id": "No.1",
     "question": "問題文...",
     "images": ["/image/25031c/25031c-1.png"]
   }
   ```

---

## ⚠️ よくあるエラーと解決方法

### エラー1: JSONファイルが読み込まれない

**原因：** JSONの構文エラー

**解決方法：**
- カンマの位置を確認
- 引用符が正しく閉じられているか確認
- JSONバリデーターで確認（https://jsonlint.com/）

### エラー2: 画像が表示されない

**原因：** 画像パスが間違っている

**解決方法：**
- 画像パスが `/image/...` で始まっているか確認
- 画像ファイルが `public/image/` ディレクトリに存在するか確認
- ファイル名が正しいか確認

### エラー3: 問題が表示されない

**原因：** `category` の値が間違っている

**解決方法：**
- `level` が `"国家1級"`, `"国家2級"`, `"国家3級"` のいずれかであるか確認
- `fuelType` が `"小型"`, `"ガソリン"`, `"ディーゼル"` のいずれかであるか確認
- `year` の形式が `"2025-1"` のようになっているか確認

### エラー4: 正解が間違っている

**原因：** `answerIndex` の値が間違っている

**解決方法：**
- `answerIndex` は1-4の範囲内である必要があります
- 1 = 選択肢(1)が正解
- 2 = 選択肢(2)が正解
- 3 = 選択肢(3)が正解
- 4 = 選択肢(4)が正解

---

## 📖 参考資料

- 詳細な説明は `src/data/questionSets/README.md` を参照してください
- テンプレートファイルは `src/data/questionSets/TEMPLATE.json` を参照してください
- 既存のファイル例を参考にしてください

---

## 🎉 完了！

これで新しい問題集を追加できます。質問がある場合は、お気軽にお問い合わせください。

